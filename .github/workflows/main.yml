name: Movieglitch Hunter

on:
    schedule:
    - cron: '*/5 * * * *'  # Must match the 5 minute window in the python script
  workflow_dispatch:      # Adds a "Run Now" button for testing

jobs:
  hunt_movies:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser google-generativeai

      - name: Run the hunter
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python movie_agent.py
        
